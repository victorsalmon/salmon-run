# 1Elasticity.ps1 — RETIRED

The `elasticity` container has been **renamed to sentry**. All functionality previously in `1Elasticity.ps1` now lives in `Skills/Docker/1Fleet.ps1` (the `SalmonRun.Fleet` module).

See:
- `Skills/Docker/1Fleet.ps1` — the current fleet runtime entrypoint
- `SalmonRun.Fleet` — the fleet module (health checks, remediation, system updates)
- `docs/Reference/Maintenance.md` — sentry lifecycle documentation
